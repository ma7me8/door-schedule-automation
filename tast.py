
'''for file in files:
    if file.name.startswith("~$"):
        continue
    print(file.name)


# should to know sheet.cell(row, col).value = gave me a value in row and col

def get_data(files_path): #first desigen fo rthe script
    for file in files_path:
        
        all_doors = {}

        for row in door_row:

            room_number = "anything"
            
            for room_number in room_numbers:
                
                
                if door_number is None:
                    continue

                door_data = {}
                
                for property_column in properties_columns:

                    property_name = "name of property"
                    property_value = "value of property in call `   

                    door_data[property_name] = property_value
                all_doors[room_number] = door_data '''
